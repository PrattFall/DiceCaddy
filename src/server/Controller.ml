(*
 * We could call these by their actual names, but these should be converted to
 * aliases with no overhead during compilation, so there is no reason _not_ to
 * make it look nice, right?
*)
module Telegram = Controller_Telegram
module NoRoute  = Controller_NoRoute
